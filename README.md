# orl.test

Simple GitHub Pages landing page for presentation attendees to access and download slide files.

## Local structure

- `index.html`: static landing page
- `styles.css`: page styling

## Next step

Replace the placeholder download links in `index.html` with the Box URLs when they are ready.
